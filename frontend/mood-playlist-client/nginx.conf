server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Proxy API to backend if needed under /api — disabled by default
  # location /api/ {
  #   proxy_pass http://backend:5001/;
  # }
}
